import CustomersClient from '@/components/CustomersClient';

export const metadata = {
  title: 'Customers — Pattabiram Sweets',
};

export default function CustomersPage() {
  return <CustomersClient />;
}
