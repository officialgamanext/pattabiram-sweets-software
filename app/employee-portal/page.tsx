import EmployeePortalClient from '@/components/EmployeePortalClient';

export const metadata = {
  title: 'Employee Portal — Pattabiram Sweets',
  description: 'Employee self-service, shift attendance logs, task checklist, and performance overview.',
};

export default function EmployeePortalPage() {
  return <EmployeePortalClient />;
}
