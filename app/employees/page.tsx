import EmployeesClient from '@/components/EmployeesClient';

export const metadata = {
  title: 'Employees — Pattabiram Sweets',
  description: 'Manage workforce records, payment modes, accepted leaves, GPS geofencing & live face reference photos.',
};

export default function EmployeesPage() {
  return <EmployeesClient />;
}

