import ItemsClient from '@/components/ItemsClient';

export const metadata = {
  title: 'Items & Categories — Pattabiram Sweets',
  description: 'Manage items, pricing, categories, units, manufacturing & packing units with compressed image uploads.',
};

export default function ItemsPage() {
  return <ItemsClient />;
}
