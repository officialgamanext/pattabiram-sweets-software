import PackingPortalClient from '@/components/PackingPortalClient';

export const metadata = {
  title: 'Packing Portal — Pattabiram Sweets',
  description: 'Box preparation queue, packaging labels, and dispatch operations.',
};

export default function PackingPortalPage() {
  return <PackingPortalClient />;
}
