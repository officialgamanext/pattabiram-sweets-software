import DashboardClient from '@/components/DashboardClient';

export const metadata = {
  title: 'Dashboard — Pattabiram Sweets',
  description: 'Business management dashboard for Pattabiram Sweets — overview of orders, sales, inventory, and employees.',
};

export default function DashboardPage() {
  return <DashboardClient />;
}
