import PayrollClient from '@/components/PayrollClient';

export const metadata = {
  title: 'Payroll — Pattabiram Sweets',
  description: 'Manage attendance with GPS geofencing & live face identification, and calculate monthly & daily salary payouts.',
};

export default function PayrollPage() {
  return <PayrollClient />;
}

