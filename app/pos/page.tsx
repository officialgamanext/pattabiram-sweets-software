import PosClient from '@/components/PosClient';

export const metadata = {
  title: 'Billing & POS — Pattabiram Sweets',
  description: 'Point of sale, barcode scanner, thermal printing, and quick billing system for Pattabiram Sweets.',
};

export default function PosPage() {
  return <PosClient />;
}
