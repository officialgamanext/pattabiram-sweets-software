import StoreClient from '@/components/StoreClient';

export const metadata = {
  title: 'Store — Pattabiram Sweets',
  description: 'Manage store locations, store codes, types, contact details, and locations for Pattabiram Sweets.',
};

export default function StorePage() {
  return <StoreClient />;
}
